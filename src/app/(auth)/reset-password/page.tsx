import { ResetPasswordForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Reset Password - Content Engine",
  description: "Reset your Content Engine password",
};

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
