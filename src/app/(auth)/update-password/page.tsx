import { UpdatePasswordForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Update Password - Content Engine",
  description: "Set a new password for your Content Engine account",
};

export default function UpdatePasswordPage() {
  return <UpdatePasswordForm />;
}
