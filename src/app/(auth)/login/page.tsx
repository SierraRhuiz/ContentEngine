import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Login - Content Engine",
  description: "Sign in to Content Engine",
};

export default function LoginPage() {
  return <LoginForm />;
}
